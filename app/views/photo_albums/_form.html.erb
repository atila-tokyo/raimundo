<%= simple_form_for photoalbum do |f| %>
    <%= f.input :title , :label => 'Nome do álbum' %> 
    <%= f.input :photos, as: :file, input_html: { multiple: true }, label: "Selectionar Fotos" %>  
    <%= f.submit value: button_name, class: 'btn btn-primary'%>
<% end %>